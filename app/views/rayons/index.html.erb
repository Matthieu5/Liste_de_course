<p id="notice"><%= notice %></p>

<h1>Liste des rayons</h1>

<table>
  <thead>
    <tr>
      <th>Nom :</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  </br>

  <tbody>
    <% @rayons.each do |rayon| %>
      <tr>
        <td><%= rayon.libelle %></td>
        <td><%= link_to 'Informations', rayon %></td>
        <td><%= link_to 'Modifier', edit_rayon_path(rayon) %></td>
        <td><%= link_to 'Supprimer', rayon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
 
<%= link_to 'Ajouter un rayon', new_rayon_path %> |
<%= link_to 'Retour', item2s_path %>